version: '3.4'

# BASE CONFIG: applies to all environments. It does not work alone

services:
  web:
    container_name: project_name-web
    volumes:
      - static:/src/project_name/static

volumes:
  static:
